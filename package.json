{
  "name": "odsh-bridge",
  "version": "1.1.0",
  "description": "OpenClaw x DeepSeek Harness bridge: gateway WebSocket client (Ed25519 pairing) + file-based envelope daemon. Zero dependencies, pure ESM.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mikoribbit/odsh-bridge.git"
  },
  "keywords": [
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "openclaw",
    "cordis"
  ],
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "oc-invoke": "src/oc-invoke.mjs",
    "oc-send": "src/oc-send.mjs",
    "oc-client": "src/oc-client.mjs",
    "bridge-daemon": "src/bridge-daemon.mjs",
    "oc-cua": "src/oc-cua.mjs"
  },
  "files": [
    "src/**",
    "docs/**",
    "config/**",
    "README.md",
    "LICENSE",
    ".env.example"
  ],
  "scripts": {
    "check": "node --check src/env.mjs && node --check src/gateway-client.mjs && node --check src/oc-invoke.mjs && node --check src/oc-send.mjs && node --check src/oc-client.mjs && node --check src/oc-cua.mjs && node --check src/bridge-daemon.mjs && bash -n scripts/setup-dsh.sh && echo ALL_SYNTAX_OK",
    "prepublishOnly": "node -e \"const fs=require('fs');const bad=['.env'];const hit=bad.filter(f=>fs.existsSync(f));if(hit.length){console.error('blocked: '+hit.join(',')+' present');process.exit(1)}\""
  }
}
